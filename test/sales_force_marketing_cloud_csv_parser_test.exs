defmodule SalesForceMarketingCloudCsvParserTest do
  use ExUnit.Case
  doctest SalesForceMarketingCloudCsvParser

  test "greets the world" do
    assert SalesForceMarketingCloudCsvParser.hello() == :world
  end
end
